import javax.swing.*;
import java.awt.*;
class Ammon extends Sprite {
    public Ammon(int x, int y) {
        super();
        image = new ImageIcon("images/ammon.png");
        setLocation(x, y);
    }
}