import javax.swing.*;
import java.awt.*;
class Robber extends Sprite {
    public Robber(int x, int y) {
        super();
        image = new ImageIcon("images/robber.png");
        setLocation(x, y);
    }
}