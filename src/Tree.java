import javax.swing.*;
import java.awt.*;
class Tree extends Sprite {
    public Tree(int x, int y) {
        super();
        image = new ImageIcon("images/tree.png");
        setLocation(x, y);
    }
}